# Visual Genetics System Dependencies for TurboShells
# Install with: pip install -r requirements_visual_genetics.txt

# Core SVG Generation
drawsvg>=1.6.0
svgwrite>=1.4.3

# SVG Rendering (alternative to cairosvg)
svglib>=1.6.0
reportlab>=4.4.0

# Image Processing
pillow>=9.0.0

# PyGame (already in main requirements, but included for completeness)
pygame-ce>=2.5.0

# Optional: For advanced SVG manipulation (requires Cairo library installation)
# cairosvg>=2.5.2  # Uncomment if Cairo is installed

# Development and Testing
pytest>=6.0.0
pytest-cov>=3.0.0
