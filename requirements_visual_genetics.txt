# Visual Genetics System Dependencies for TurboShells
# Install with: pip install -r requirements_visual_genetics.txt

# Core SVG Generation
drawsvg>=1.6.0
svgwrite>=1.4.3

# Image Processing
pillow>=9.0.0

# PyGame (already in main requirements, but included for completeness)
pygame-ce>=2.5.0

# Optional SVG Rendering Libraries (hybrid renderer will use best available)
# svglib>=1.6.0          # Requires Cairo library
# reportlab>=4.4.0       # Requires Cairo library  
# wand>=0.6.13          # Requires ImageMagick
# svg2png>=1.2          # Simple SVG to PNG conversion
# cairosvg>=2.5.2       # Requires Cairo library

# Development and Testing
pytest>=6.0.0
pytest-cov>=3.0.0

# Note: The hybrid renderer automatically detects available libraries
# and uses the best one. Install any of the optional libraries above
# for better SVG rendering quality.
